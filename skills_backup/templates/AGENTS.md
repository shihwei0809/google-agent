# <專案名稱>（由 Gem 升級而來）

一條「給材料 → 固定流程處理 → 輸出成品」的可重複工作流。
**固定的是流程＋規格（凍在 Skill / 本檔 / scripts），浮動的是材料（每次丟 `input/`）。**

## 固定偏好（每次套用）
- 回應一律用繁體中文。
- <填入這個 Gem 原本的固定規格：格式、語氣、標準…>
- 成品放 `output/`。

## 怎麼用
1. 把這次的材料丟進 `input/`。
2. 執行對應的 `/<workflow>`。
3. 到 `output/` 取成品。

## 生圖（若需要）
本流程的生圖步驟寫成抽象指令「產生一張圖：<描述>」，由當前 agent 的生圖方式處理
（Antigravity→Nano Banana Pro／Codex→image 2／Claude·OpenCode→API）。
