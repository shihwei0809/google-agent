---
name: <skill-名稱>
description: 當使用者要「<觸發情境，越精準越好>」時使用。<一句話說明這個 skill 做什麼、吃什麼材料、產出什麼成品>。
---

# 角色
<把原本 Gem 的系統指令貼進來，調整成「對 input/ 材料執行」的口吻>

# 鐵則（固定不變）
- <原 Gem 的固定規格：格式、標準、語氣…>
- 全程繁體中文。
- 成品放 `output/`。

# 輸入（材料，每次浮動）
讀取 `input/` 內的材料：<可能的形態：主題、PDF、資料夾、題庫…>

# 流程
1. <步驟一>
2. <步驟二：需要算的事 → 呼叫 scripts/ 的 Python，不要叫模型硬算>
3. <步驟三：需要圖 → 「產生一張圖：<描述>」，交給當前 agent 的生圖方式>
4. <步驟四：輸出 .docx/.xlsx/.html… 到 output/>

# 限制
- <不可踰越的邊界>
