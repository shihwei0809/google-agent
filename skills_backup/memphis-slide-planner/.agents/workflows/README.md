---
name: memphis-slide-planner
description: 一鍵執行大綱至孟菲斯風格投影片的轉換並輸出
---

# /memphis-ppt
請載入 `memphis-slide-planner` Skill，執行簡報生成流程：
1. 確保 `input/slide_outline.txt` 內已寫入投影片大綱。
2. 執行命令 `python scripts/generate_memphis_ppt.py`。
3. 將生成的 `.pptx` 簡報存入 `output/`。
4. 執行完回報：投影片張數與產出位置。
