# =====================================================================
# 🚀 N系列BARCODE現場檢點入庫核對 專案環境一鍵設定腳本 (setup_env.ps1)
# =====================================================================

Write-Host "==================================================" -ForegroundColor Cyan
Write-Host "🎨 檢查「N系列BARCODE現場檢點入庫核對」專案環境..." -ForegroundColor Cyan
Write-Host "==================================================" -ForegroundColor Cyan

Write-Host "✓ 本專案核心為 Google Apps Script 雲端後端程式碼 (Code.gs)。" -ForegroundColor Green
Write-Host "✓ 條碼雙軌相容版本：v51.1 (支援 20 碼舊庫存 & 24 碼新標籤)。" -ForegroundColor Green
Write-Host "✓ 檔案已就緒，可直接部署至 Google Apps Script 編輯器。" -ForegroundColor Green

Write-Host "`n🎉 環境檢查完畢！" -ForegroundColor Cyan
