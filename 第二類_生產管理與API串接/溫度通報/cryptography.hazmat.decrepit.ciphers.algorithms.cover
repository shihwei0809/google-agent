       # This file is dual licensed under the terms of the Apache License, Version
       # 2.0, and the BSD License. See the LICENSE file in the root of this repository
       # for complete details.
       
    1: from __future__ import annotations
       
    1: import warnings
       
    1: from cryptography import utils
    1: from cryptography.hazmat.primitives._cipheralgorithm import (
           BlockCipherAlgorithm,
           CipherAlgorithm,
           _verify_key_size,
       )
       
       
    2: class ARC4(CipherAlgorithm):
    1:     name = "RC4"
    1:     key_sizes = frozenset([40, 56, 64, 80, 128, 160, 192, 256])
       
    1:     def __init__(self, key: bytes):
               self.key = _verify_key_size(self, key)
       
    2:     @property
    2:     def key_size(self) -> int:
               return len(self.key) * 8
       
       
    2: class TripleDES(BlockCipherAlgorithm):
    1:     name = "3DES"
    1:     block_size = 64
    1:     key_sizes = frozenset([64, 128, 192])
       
    1:     def __init__(self, key: bytes):
               if len(key) == 8:
                   warnings.warn(
                       "Single-key TripleDES (8-byte keys) is deprecated and "
                       "support will be removed in a future release. Use 24-byte "
                       "keys instead (e.g., key + key + key).",
                       utils.DeprecatedIn47,
                       stacklevel=2,
                   )
                   key = key + key + key
               elif len(key) == 16:
                   warnings.warn(
                       "Two-key TripleDES (16-byte keys) is deprecated and "
                       "support will be removed in a future release. Use 24-byte "
                       "keys instead (e.g., key + key[:8]).",
                       utils.DeprecatedIn47,
                       stacklevel=2,
                   )
                   key = key + key[:8]
               self.key = _verify_key_size(self, key)
       
    2:     @property
    2:     def key_size(self) -> int:
               return len(self.key) * 8
       
       
       # Not actually supported, marker for tests
    2: class _DES:
    1:     key_size = 64
       
       
    2: class Blowfish(BlockCipherAlgorithm):
    1:     name = "Blowfish"
    1:     block_size = 64
    1:     key_sizes = frozenset(range(32, 449, 8))
       
    1:     def __init__(self, key: bytes):
               self.key = _verify_key_size(self, key)
       
    2:     @property
    2:     def key_size(self) -> int:
               return len(self.key) * 8
       
       
    2: class CAST5(BlockCipherAlgorithm):
    1:     name = "CAST5"
    1:     block_size = 64
    1:     key_sizes = frozenset(range(40, 129, 8))
       
    1:     def __init__(self, key: bytes):
               self.key = _verify_key_size(self, key)
       
    2:     @property
    2:     def key_size(self) -> int:
               return len(self.key) * 8
       
       
    2: class SEED(BlockCipherAlgorithm):
    1:     name = "SEED"
    1:     block_size = 128
    1:     key_sizes = frozenset([128])
       
    1:     def __init__(self, key: bytes):
               self.key = _verify_key_size(self, key)
       
    2:     @property
    2:     def key_size(self) -> int:
               return len(self.key) * 8
       
       
    2: class IDEA(BlockCipherAlgorithm):
    1:     name = "IDEA"
    1:     block_size = 64
    1:     key_sizes = frozenset([128])
       
    1:     def __init__(self, key: bytes):
               self.key = _verify_key_size(self, key)
       
    2:     @property
    2:     def key_size(self) -> int:
               return len(self.key) * 8
       
       
    2: class Camellia(BlockCipherAlgorithm):
    1:     name = "camellia"
    1:     block_size = 128
    1:     key_sizes = frozenset([128, 192, 256])
       
    1:     def __init__(self, key: bytes):
               self.key = _verify_key_size(self, key)
       
    2:     @property
    2:     def key_size(self) -> int:
               return len(self.key) * 8
       
       
       # This class only allows RC2 with a 128-bit key. No support for
       # effective key bits or other key sizes is provided.
    2: class RC2(BlockCipherAlgorithm):
    1:     name = "RC2"
    1:     block_size = 64
    1:     key_sizes = frozenset([128])
       
    1:     def __init__(self, key: bytes):
               self.key = _verify_key_size(self, key)
       
    2:     @property
    2:     def key_size(self) -> int:
               return len(self.key) * 8
