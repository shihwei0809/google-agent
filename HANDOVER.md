# 跨電腦交接日誌 (HANDOVER)

## 2026-09-04 交接事項 (最新進度：下午場收工)
- **完成事項**：
  1. **資材課 PPT 公版合併**：將進出貨作業流程 PPT 成功完美合併至鴻勝公版模板，使用原生 Windows COM (PowerPoint Automation) 自動化腳本解決了 `python-pptx` 直接操作 XML 造成的損毀問題。
  2. **本地三合一單 Excel 跨分頁掃描 (`三合一單自動產生器/main.py`)**：將原先僅讀取單一預設分頁全面升級為遍歷 `wb.worksheets` 所有工作表，解決多分頁排程僅抓取首頁少數資料之問題。
  3. **匯入預覽視窗升級多欄專業表格 (`ttk.Treeview`)**：重構 `ImportRangeDialog`，徹底解決右側地點被截斷問題。提供 8 大獨立欄位（項次、分頁、出貨日期、到廠時間、批號、槽號、地點、長代號），欄寬可自由拖拉，視窗加大至 1080x700，支援視窗拉伸與水平/垂直雙向滾動，並加入斑馬紋美化。
  4. **修復「槽號」誤判為日期之問題**：排除標題含「日/期/時間/TIME/DATE/到廠/出車」之欄位，並加上槽號格式檢驗與批號（如 `26902E3191`）自動精準推算（`E319`）防呆。
  5. **主畫面視窗尺寸調整**：主視窗調整為 1200x780（最小限制 1050x600），下方表格容器新增水平滾動條，任何解析度螢幕皆能 100% 完整顯示。
  6. **遵循使用者指示**：本次變更嚴格僅修改 `D:\GOOGLE ANGET\三合一單自動產生器`，`勝一三合一單產生系統` 維持原狀未更動。
- **遇到問題與解決**：
  - **PPTX 損毀**：`python-pptx` 移除投影片時易造成 ZIP/XML 結構損毀，改用 `win32com.client` 呼叫本機 PowerPoint 直接 `ApplyTemplate` 完美解決。
  - **槽號欄位誤判**：Excel 中「槽車出車日」含有「槽車」關鍵字，導致 `tank_col` 誤抓日期，已於標題搜尋時排除日期/時間關鍵字，並於資料提取時加入 3~6 碼英數驗證與 `get_tank_from_batch()` 自動推算。
  - **預覽畫面截斷**：原 Listbox 單行文字過長且無水平滾動條，改用 `ttk.Treeview` 多欄獨立呈現與雙向滾動條。
- **後續未完進度**：
  - 本次進度暫存於 `feat/20260904` 分支做跨機備份，尚未發布至 `main`。請於下次開工時接續測試本地三合一產生器，確認無誤後再合併發布上線。
