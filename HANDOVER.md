# 跨電腦交接日誌 (HANDOVER)

## 2026-09-07 夜間收工交接 (狀況 A 純備份：QC 看板 GAS 雲端雙向連動、筆電滿版自適應與 Cloudflare Pages 正式上線)
- **完成項目**：
  1. **Google Apps Script 雲端試算表真實雙向連動**：
     - 正式嵌入並打通 GAS Web App API（`https://script.google.com/macros/s/AKfycbxRK8do1QBaYC8cDgfXDgWt-l9R4Dt6Ig1OQ_-8T1eJT50NiqGfSYWYGGh9hY0HWaPy/exec`）。
     - 解決 CORS 跨域 OPTIONS 預檢錯誤，採用 `text/plain;charset=utf-8` 達成送樣登記、放行判試合格、逾期巡檢全自動雙向寫入與讀取。
     - 支援動態讀取 Google 試算表 `System_Config` 分頁：等級、動向、單位與 25 種化學品品名完全自雲端即時載入，未來單位增加直接相容。
     - Teams Webhook 升級為單位動態路由 (`TEAMS_WEBHOOK_[單位名稱]`)，各課專屬頻道精準分流。
  2. **筆電免左右捲動之 4 欄智慧自適應排版**：
     - 徹底解決筆電螢幕左右水平捲軸痛點，實作自適應複合欄排版：
       - 左側待檢：品項/單號 (30%)、槽號/車牌 (23%)、送樣單位/等候時長 (28%)、操作 (19%)。
       - 右側已檢：品項/單號 (30%)、槽號/車牌 (23%)、送樣單位/判定 (28%)、判定備註 (19%)。
     - 100% 滿版無捲軸，並支援右上角「⚡ 自適應 / 📋 傳統九欄」一鍵切換。
  3. **T100 排程匯入快取**：
     - 上傳之 Excel 槽車排程自動暫存至 `localStorage`，重新整理或重開 App 排程不丟失。
  4. **Cloudflare Pages 獨立專屬網址上線與 Token 永久固化**：
     - 排查出 9/5 的 API Token 僅儲存於臨時變數中，已將其永久寫入 Windows 使用者環境變數 (`CLOUDFLARE_API_TOKEN`) 與 `.env`。
     - 成功透過 Wrangler 建立並部署至獨立專屬網址：**`https://qc-samples.pages.dev/`**。
     - 專案大廳同步更新卡片啟動連結，PWA 離線快取與各端 App 安裝能力 100% 驗證通過。
- **明日接手待辦事項 (使用者指示)**：
  - **圖一「🧪 本機模擬測試箱」權限管控**：
    - 使用者指出首頁頂部的「🧪 本機模擬測試箱」工具列（注入逾期樣品、執行逾期巡檢、Teams Webhook 設定、標籤機設定）目前任何人進來皆可點擊，需增加權限卡控機制（如管理員密碼/金鑰解鎖、隱藏模式或僅在特定網址參數/本機環境顯示），避免現場人員誤觸造成資料混亂。

## 2026-09-07 正式上線發布 (勝一出貨排程管理系統：Web Push 推播、SSE 雙向已讀即時連動、Render 雲端部署與固定穿透)
- **完成項目**：
  1. **Web Push 背景推播與通知點擊跳轉聚焦**：
     - 生成 VAPID 金鑰並實作 `POST /api/subscribe` 與背景推播分發模組。
     - 整合 Service Worker (`sw.js`)，支援排程異動（時間/日期/地點/充填手更換）自動發送推播通知至同仁手機。
     - 點擊通知自動喚醒系統並聚焦開啟單筆訂單確認彈窗，記錄已讀人員與時間戳記。
  2. **SSE (Server-Sent Events) 雙向即時已讀連動與 3 秒心跳輪詢**：
     - 解決手機端標記已讀後電腦端未即時更新問題，加入 `/api/events` 毫秒級廣播通道。
     - 技服人員在手機開啟訂單確認已讀，電腦端管理大表 0.1 秒內自動切換為「🟢 已讀 (時間/同仁)」。
     - 手機端日程卡片同步即刻重繪，徹底防止現場人員誤判。
  3. **技服人員個人化日程查詢與 35 組標準帳號對齊**：
     - 技服同仁以姓名登入後自動鎖定身分，預設顯示「當天與明天」排程，提供「之前歷史排程」與「全部日程」一鍵切換按鈕。
     - 嚴格綁定 Excel 中 35 組官方帳號名冊，杜絕任何測試字串或標題雜訊。
  4. **Render 雲端 + Cloudflare / 本機雙軌部署架構**：
     - 新增 `Dockerfile`（Node.js 20 + Python 3 條碼生成環境），支援 Render 雲端免費平台一鍵 24 小時免開機常駐。
     - 本機內建強制固定子網域穿透鎖定 (`https://shinychem-ipahq.loca.lt`)，廠內亦可直接經 WiFi (`http://192.168.0.15:3000`) 極速連線。
     - 補齊專案標準四件套（`README.md`、`SKILL.md`、`setup_env.ps1`、`build_manual_doc.py`）並產出 Word 操作手冊。
- **遇到的問題與解決**：
  - **手機端已讀後電腦未同步**：原因為電腦端為靜態快取。解法：建置後端 SSE 廣播通道與前端 3 秒心跳自動校正，達成跨裝置毫秒同步。
  - **點擊儲存報錯 `Cannot destructure property 'order' of 'req.body'`**：加入防快取中介層時誤移 `express.json()`。解法：補回 `express.json({ limit: '50mb' })` 與 urlencoded 解析。
  - **Localtunnel 隨機分發動物名稱**：公用伺服器在舊連線未釋放時會派發隨機名稱。解法：在 `server.js` 寫入強制鎖定重試迴圈，非指定子網域立即釋放重試，直到鎖定為止。

## 2026-09-07 收工交接 (正式上線發布：QC 看板 T100 槽車排程智慧帶入與 PWA 雙軌架構)
- **完成項目**：
  1. **T100 槽車排程智慧自動帶入 (20 車次完整提取)**：
     - 路徑：`d:\GOOGLE ANGET\第二類_生產管理與API串接\QC-系統客製化電子化工廠`。
     - 專門鎖定《明天進出貨排程報表(全部)20x3A00.xlsx》中的「明天進出貨排程報表(槽車)」分頁。
     - 實作動態逐列狀態機掃描，自動跨越空白列，同時提取上半部「出貨槽車 (10 車次)」與下半部「進料槽車 (10 車次)」，共 20 筆完整槽車排程。
     - 正則抽取 ISO Tank 櫃號（如 `PPCU7019228`）與台積電各廠別回收槽車號（如 `KEQ-3506 (櫃:6108)`）。
     - 前端表單上方整合「🚛 T100 槽車排程」下拉選單，點選車次自動將單號、動向、等級、品名、槽號、櫃號/車牌、數量瞬间全部自動填滿，1 秒送樣！
     - 支援前端 SheetJS 即時上傳 / 拖曳新 Excel 檔案，秒級刷新當日排程。
  2. **出通/入庫單號非必填化（相容提前充填庫存備料實務）**：
     - 解除單號必填限制，現場提前充填庫存若留空不填，系統自動補全 `[YYYYMMDD]-庫存`，兼顧現場零負擔與檢驗標籤/LINE廣播之數據追溯性。
  3. **PWA 雙軌標準架構實施 (方案一)**：
     - 建立 `1_Web_網頁版/` 與 `2_PWA_App版/`，配齊 `manifest.json`、`sw.js`、高解析圖示 (`icons/`)。
     - 本機測試支援：`run_server.py` + `啟動PWA本機測試.bat` (純 ASCII，自動尋找可用 Port 並顯示 LAN IP)。
     - 產出《鴻勝化學_QC檢驗即時看板系統_操作手冊.docx》並以 `git add -f` 強制納管。
- **遇到的問題與解決**：
  - **中間空白行導致漏抓下半部進貨**：Excel 槽車工作表以第 15 列空白分隔上半部「出貨通知單」與下半部「入庫單」。解法：全面改為動態逐列狀態機掃描，偵測到不同標題列自動切換模式，出貨與進貨 20 車次全數精準提取。
  - **提前充填庫存無單號問題**：現場需充填庫存備料，無出通/入庫單號。解法：單號改選填，留空自動賦予 `日期-庫存` 編號，兼顧現場便利與檢驗數據可追溯性。

## 2026-09-07 進度記錄 (N系列入庫現場檢點：20碼舊庫存與24碼新標籤雙軌相容)
- **完成項目**：
  1. **建立獨立「N系列BARCODE現場檢點入庫核對」專案**：
     - 路徑：`d:\GOOGLE ANGET\第二類_生產管理與API串接\N系列BARCODE現場檢點入庫核對`。
     - 嚴格隔離「入庫現場檢點」與「出貨核對」，杜絕兩者業務與檢驗邏輯混淆。
  2. **GAS 後端 v51.1 條碼雙軌相容與 M76 儲槽智慧卡控**：
     - 自動自各欄位掃描之批號（桶批號或 4in1 批號）中偵測槽號代碼 `M76`。
     - 若為 `M76` 儲槽：卡控新規格 24 碼（過渡期相容 20 碼舊庫存）。
     - 若為其他非 `M76` 儲槽：嚴格限定維持 20 碼，防止其他產線誤掃 24 碼標籤。
     - 動態計算料號與有效期限截取位移（20 碼取 1~10 碼料號與 10~18 碼效期；24 碼取 1~14 碼料號與 14~22 碼效期）。
     - 結尾公司代碼卡控放寬為相容 `TS` 與 `TW`。
     - 歷史查詢 `searchRecords` 同步支援雙軌動態截取，保障試算表歷史紀錄料號不失真。
  3. **完成原始 v51.0 備份與專案標準規範**：
     - 留存 `Code_v51.0_原始備份.gs`。
     - 配齊 `README.md`（含入庫 vs 出貨差異對照表、20/24碼對照表、M76 儲槽分流說明）、`SKILL.md` 與 `setup_env.ps1`。
- **遇到的問題與解決**：
  - **現場掃描報錯 `長度錯誤！首碼1應為20碼。目前長度: 24`**：因新標籤料號擴充至 13 碼（總長 24 碼），原邏輯寫死 20 碼被阻擋。解法：依據批號中的儲槽代碼 `M76` 實施分流驗證。

## 2026-09-05 收工交接 (正式上線發布：PWA 雙軌架構實施與 eshine-package 上線)
- **完成項目**：
  1. **全專案 PWA 雙軌架構標準化 (PWA Dual-Track Architecture)**：
     - 在 `AGENTS.md` 與中央大腦 `INSTRUCTIONS.md` 確立 Rule 7 (純 ASCII BAT) 與 Rule 8 (PWA 雙軌 `1_Web_網頁版` / `2_PWA_App版`) 開發規範。
     - 重構「倉庫常用包材管理」與「N系列BARCODE出貨核對」專案，拆分雙軌目錄並配齊 `manifest.json`、`sw.js`、高解析圖示 (`icons/`)、`run_server.py` 與 `一鍵部署到Cloudflare.bat`。
  2. **鴻勝包材管理系統 v13.5 (Eshine Package) 雲端正式上線**：
     - 整合即時 GAS Web App 後端與高質感藍灰 Bootstrap 5 介面。
     - 透過 Wrangler Direct Upload 成功部署至 Cloudflare Pages：**`https://eshine-package.pages.dev`**。
     - 具備離線快取防呆與三端 (Windows/Mac 電腦獨立視窗、Android 桌面圖示、iOS Safari 加入主畫面) PWA 獨立 App 安裝能力。
  3. **Git 倉庫健全性修復 (Submodule / Gitlink 清理)**：
     - 清除 Git 索引內殘留之 22 個無效 `160000` gitlink 孤立指標，徹底解決 Cloudflare 雲端 build clone 失敗問題。
  4. **全套圖文手冊產出與 Git 強制納管**：
     - 產出《鴻勝包材管理系統_操作手冊》與《Cloudflare_Pages_完整部署與疑難排解手冊》之 Word (.docx) 及 PDF (.pdf) 文件，並以 `git add -f` 強制納管。
- **遇到的問題與解決**：
  - **Windows CMD BAT 亂碼 (`嚜濃echo`)**：UTF-8 BOM 導致 CMD 報錯。解法：全面改為純 ASCII 3 行 BAT (`@echo off\npython run_server.py\npause`)，控制台繁體中文輸出全權由 Python 接管。
  - **瀏覽器本機中文亂碼 (`暾餃蝠ㄥ蝟餌紋`)**：HTML 與伺服器缺少 utf-8 編碼宣告。解法：注入 `<meta charset="UTF-8">` 並在伺服器回應標頭設定 `Content-Type: text/html; charset=utf-8`。
  - **Cloudflare Git Clone 報錯**：`fatal: No url found for submodule path in .gitmodules`。解法：使用 `git update-index --force-remove` 將所有 160000 虛擬指標從 Git 索引剔除。
  - **Cloudflare 免費方案佇列排隊**：雲端 Build 需排隊等容器。解法：改用 Direct Upload (Wrangler API Token)，3 秒直傳全球邊緣節點。

## 2026-09-04 深夜收工 (正式上線發布：線上地點代號回寫主機端功能)
- **完成項目**：
  1. **線上網頁端地點代號即時回寫主機電腦端**：
     - 在 `server.py` 實作 `/api/save_location` 與 `/api/delete_location` API 端點。
     - 線上同仁無論使用手機、平板或遠端瀏覽器新增地點時，系統自動將資料同步寫入電腦端 `地點代號對照表.xlsx`（包含三合一單網頁架機伺服器、三合一單自動產生器、勝一三合一單產生系統多端同步）。
     - 網頁端排程表格即時反應更新，免重新整理頁面；本機桌面版點擊「重新載入」亦可立即同步。
  2. **未知地點智慧點擊回寫導航 (Zero-Friction UX)**：
     - 表格排程遇到未建檔地點時，長代碼欄位呈現紅色指針按鈕樣式 `❌ 未知代號 (點此回寫新增)`。
     - 點擊後自動彈跳地點維護視窗並預填短地點代碼，同仁只需輸入長代號並點擊儲存，表格全列立即刷新。
  3. **UI 介面最佳化與按鈕收斂**：
     - 移除工具列多餘折行按鈕，將 `📍 地點代號維護 (回寫電腦端)` 統一整合至綠色連線狀態列右側，工具列恢復單行整齊排列。
  4. **全廠區 30 處地點對照碼鏡像整合**：
     - 整合並同步 12P、14P、15P、18P、AP、20P、22P 等 30 處完整對照表至全專案 Excel 檔案。
  5. **時間格式正規化防呆**：
     - 解決 SheetJS 將 Excel 純時間小數解析為 1899-12-30 導致小輸入框顯示 `SAT DEC 30` 之問題，前後端全面保證純 4 碼時間輸出（如 `0900`, `1400`, `1600`）。
- **遇到的問題與解決**：
  - **線上與電腦端對照表割裂**：線上人員無法登入主機手動開 Excel。解法：建立後台檔案安全寫入與多專案鏡像回寫機制。
  - **工具列折行不美觀**：多加按鈕導致第二行破版。解法：收斂整合至綠色連線狀態列右側，維持工具列清爽。
  - **Excel 小數時間字串化破版**：SheetJS cellDates 解析為 Date 物件在字串化時包含星期與日期。解法：封裝 `normalizeTimeStr` 函式精準提取純 4 碼時間。

## 2026-09-04 晚間交接事項 (正式上線發布：main + Tag)
- **完成項目**：
  1. **三合一單檔名規則更新（出貨日 + 槽號 + 廠別）**：
     - 原命名：2026.8.18. 18P3B台積電槽車barcode三合一單.xlsx
     - 新命名：2026.8.18. E44 18P3B台積電槽車barcode三合一單.xlsx
     - 本地桌面版 (main.py) 與 網頁版 (server.py) 全面同步支援。
  2. **單列生產履歷 Excel 獨立產生功能 (Chemical_Lorry)**：
     - 原檔命名後半段更新：在廠別前加入槽號，如 Chemical_Lorry_...勝一化工股份有限公司-0905 E44 15P5.xlsx。
     - 解決 ws.delete_rows() 處理 1897 列超大範本當機效能問題：改採全新工作表精準建構法 (uild_single_row_lorry_workbook)，在 1 秒內極速完成產出。
     - 視窗視角修復：移除 	opLeftCell='A7' 強制捲動偏移，確保打開 Excel 時從第 1 列至第 7 列完整顯示，表頭與排程一目了然。
     - 自動欄寬與高度最佳化：依據欄位字串長度與表頭長度自動計算最佳欄寬（21~35），文字不再被遮蔽或折行，第 6 列表頭高度擴大至 28.0。
  3. **UI 介面排版優化與報表勾選分離**：
     - 本地桌面版：工具列左側放置資料載入按鈕，右側放置表格列操作按鈕；獨立出「欲產生的報表勾選」區域，支援單獨或一鍵產出三合一單、運輸通知表、生產履歷 Excel。
     - 新增「📋 載入生產履歷 (Chemical_Lorry)」獨立按鈕與檔案路徑狀態顯示。
  4. **修復桌面端 UnboundLocalError 語法問題**：
     - 清除函式內部所有巢狀 import，將 os、
e、glob、json、csv 等標準函式庫全面提升至頂層，徹底解決點擊按鈕無反應問題。
  5. **正式合併至 main 並發布 Tag**：
     - 經測試驗證通過，將 eat/20260904 完整合併至 main，並升級發布新版本 Tag。
- **遇到的問題與解決**：
  - **Openpyxl 刪除列效能瓶頸**：範本含有全欄格式導致 max_column=16384，ws.delete_rows() 會極度消耗 CPU 卡死。解法：改用新工作表只複製前 30 欄及目標列資料，1 秒內生成完畢。
  - **Excel 打開視角遮蔽表頭**：因誤設 	opLeftCell='A7' 導致畫面捲動遮蔽第 1~6 列表頭。解法：保持預設 A1 視角並保留 A7 凍結窗格。
  - **欄位文字被切斷**：原欄寬為預設 13，導致 PM 日期與批號被遮擋。解法：動態計算字元長度並自動設定最佳欄寬。
  - **Tkinter UnboundLocalError**：函式中途放置 import os 導致變數作用域被鎖定為 local。解法：全域重構清除所有巢狀 import。

## 2026-09-04 日間交接事項 (進度：三合一單自動產生器與教材系統)
- **完成項目**：
  1. 取得勝一教育訓練 PPT，使用 win32com 完成範本套用轉換。
  2. 桌面版 Excel 讀取優化，支援讀取全工作表資料。
  3. 匯入範圍預覽對話框，多欄位 Treeview 瀏覽。
  4. 批號自動解析槽號、長代碼對照與鍵盤上下左右導航。
